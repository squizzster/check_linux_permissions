# check_linux_permissions
Best-effort Linux simulator for recursive delete and writability checks. It predicts whether paths are removable, writable, or either, without modifying file contents or metadata. Supports labeled path output, home-path suppression, read-only/special-fs detection, and clean pipe/Ctrl-C exits.
